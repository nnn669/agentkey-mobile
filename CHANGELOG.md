# 变更日志

本项目遵循 Keep a Changelog 的可读性原则；版本号使用语义化版本格式。

## [1.0.0] - 2026-08-15

### 新增

- Android 优先的 AgentKey 工作台，包含自定义 API 供应商、多模型管理和模型独立的多密钥池。
- 密钥优先级、严格循环与最少负载路由，连通性诊断和自动冷却机制。
- HTTP 与 SSE 远程 MCP 服务发现、调用前参数脱敏授权、高风险工具记住授权和工具级撤销。
- 本地记忆库、任务技能匹配、本地技能中心与脱敏 JSON 技能包的导入、导出、合并和覆盖恢复。
- 当前任务及全局 Token 统计，支持真实 usage 对比、本地估算、日期、模型和供应商筛选趋势。
- 虚拟工作区上的受限白名单终端，包含逐次审阅、审计记录和可选的低风险自动批准。

### 安全

- API 密钥和 MCP 认证令牌仅保存于设备本地安全存储；界面和导出内容均采用脱敏展示。
- MCP 调用与沙盒终端遵循显式授权及最小权限边界。

### 验证

- 通过 TypeScript 类型检查、Vitest 逻辑测试、Expo ESLint 与 Android 静态导出验证。
