# 贡献指南

感谢你关注 AgentKey。该项目是一个移动端 Agent 工作台原型；欢迎通过缺陷修复、可测试的功能增强、文档改进和安全设计建议参与协作。

## 开始之前

请先阅读根目录的 [README](README.md) 与 [安全策略](SECURITY.md)。涉及 API 密钥、远程 MCP 服务、记忆数据或终端授权边界的改动，必须说明其数据流、最小权限设计和潜在失败模式。请勿提交真实 API 密钥、认证令牌、个人数据、设备导出文件或生产 MCP 地址。

## 本地开发

使用 Node.js 22+ 和 npm 安装依赖。所有变更在提交前应通过下列校验：

```bash
npm ci
npm run check
npm run test
npm run lint
npx expo export --platform android
```

项目使用严格 TypeScript。与状态、密钥路由、MCP 参数处理、技能包格式或受限终端有关的纯逻辑变动，必须同步添加或更新 Vitest 测试。

## 提交流程

请从最新的 `main` 分支新建主题分支，并保持每个拉取请求只解决一个清晰问题。拉取请求应说明目标、主要实现、已执行的验证命令和必要的手动测试步骤。若用户界面有明显变化，请提供截图或录屏说明。

维护者会关注以下事项：功能是否保持本地优先与最小权限边界、错误是否有清晰的用户反馈、隐私敏感字段是否脱敏，以及 Android 导出是否仍然可用。自动化工作流会在推送和拉取请求时运行类型检查、测试、规范检查和 Android 静态导出。

## 行为准则

请以尊重、建设性和包容的方式讨论问题。针对代码和设计提出意见，而不是针对个人。涉及安全问题时，请遵循 [安全披露流程](SECURITY.md)，不要公开泄露可被利用的细节。
