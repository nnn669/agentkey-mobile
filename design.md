# AgentKey 多模型代理：移动端界面设计

## 产品范围与安全边界

本版本定位为**安卓优先的本地交互原型**：用户可以在设备内管理自定义 API 供应商、模型与多个密钥，并配置优先级、轮询及故障切换策略。界面以本地演示数据呈现调度结果；不会在客户端预置真实服务商密钥，也不会将用户输入的密钥写入日志或远端服务。

真实模型执行可采用两条后续路径，均保留给用户按安全与部署要求选择。

| 方案 | 运行方式 | 优点 | 取舍 | 配置复杂度 |
| --- | --- | --- | --- | --- |
| 设备直连 | 应用在获得用户同意后，直接调用其自定义兼容接口 | 启动快、无需中转服务、适合个人受控接口 | 密钥安全依赖设备安全能力；不适合共享或集中审计 | 较低 |
| 安全代理层 | 应用将请求交给用户自有的受控服务，再由该服务调用模型 API | 便于密钥托管、速率控制、审计和多设备使用 | 需要部署服务端与访问控制 | 较高 |

## 设计原则

界面以 **9:16 竖屏** 和单手操作为基准。主导航固定在底部；最常用的“新建 API”“添加密钥”和“运行任务”位于拇指可达区域。信息密度保持克制，以状态、队列、配额和切换原因作为核心反馈，不在首屏暴露敏感明文。

## 页面清单

| 页面 | 核心内容 | 主要操作 |
| --- | --- | --- |
| 工作台 | 当前代理状态、默认模型、可用密钥池、今日调用量、最近任务 | 发起演示任务、查看调度规则、进入配置页 |
| 代理任务 | 多轮任务输入、步骤列表、工具/API 执行轨迹、最终结果 | 运行、停止、重新执行、复制结果 |
| API 供应商 | 供应商卡片、基础 URL、兼容格式、启用状态、模型数量 | 添加、编辑、停用、删除供应商 |
| 模型与密钥池 | 每个模型的候选密钥、优先级、健康状态、配额、轮询模式 | 添加密钥、隐藏/显示末四位、调整策略、测试连通性 |
| 调度规则 | 默认模型、轮询顺序、失败阈值、冷却时间与故障切换路径 | 切换“优先级/轮询/最少负载”策略 |
| 设置 | 本地数据说明、敏感操作二次确认、原型模式说明 | 清除演示数据、导出脱敏配置 |

## 关键用户流程

### 新增自定义 API

用户从“API”标签点击“添加供应商”，填写显示名称、基础 URL 和接口兼容格式，随后在“模型与密钥池”中添加模型名称及其密钥条目。保存后，该供应商会在工作台的可选模型列表中出现。

### 配置多密钥轮询与故障切换

用户打开模型详情，为同一个模型保存多个标签化密钥，并给每项设定优先级与月度配额。用户随后在“调度规则”中选择优先级优先、循环轮询或最少负载模式；模拟失败后，界面会解释切换到备用密钥的原因。

### 运行代理任务

用户在工作台输入任务并点击“运行代理”。任务页依次显示计划、选择的模型、密钥池的脱敏标识、调用结果或故障切换状态；在原型模式下，这一流程使用确定性的本地演示事件，不发送真实网络请求。

## 数据模型

| 实体 | 关键字段 | 用途 |
| --- | --- | --- |
| Provider | id、name、baseUrl、protocol、enabled、models | 自定义 API 供应商配置 |
| ModelProfile | id、providerId、modelId、label、enabled | 模型选择与展示 |
| KeyEntry | id、modelProfileId、label、secret、priority、quota、usage、status、lastError | 多密钥池与健康状态 |
| RoutingRule | modelProfileId、strategy、failureThreshold、cooldownSeconds | 决定密钥轮询与切换方式 |
| AgentRun | id、prompt、status、steps、modelLabel、keySuffix、createdAt | 代理任务的脱敏执行记录 |

## API 连通性与自动冷却

连通性测试提供两种明确区分的模式。**模拟诊断**使用确定性规则快速演示成功、认证失败、超时或限流状态，不会读取密钥明文或发出网络请求。**真实直连测试**仅在用户已为该供应商保存密钥且主动点击后执行；它会向供应商的兼容模型端点发起一次轻量请求，所有结果只记录脱敏的 HTTP 状态、耗时和错误类别，不保存响应正文或密钥。

| 场景 | 触发条件 | 动作 | 用户可见反馈 |
| --- | --- | --- | --- |
| 连通性成功 | 收到 2xx 响应 | 更新供应商为已验证，记录延迟 | “可连接”、测试时间和延迟 |
| 认证失败 | 收到 401 或 403 响应 | 标记该密钥为不可用并进入冷却 | “认证失败”，显示脱敏尾号与原因 |
| 限流或配额触达 | 收到 429 或使用量达到配额 | 设置冷却截止时间，暂时从路由候选中移除 | “冷却中”，显示剩余时间 |
| 网络超时或 5xx | 连续失败达到阈值 | 对当前密钥启动冷却并路由至备用候选 | “已自动切换”，显示切换原因 |
| 冷却到期 | 路由读取或界面刷新时发现截止时间已到 | 自动恢复为可用状态并清除临时错误 | “已自动恢复” |

`Provider` 增加 `health`、`lastTestedAt`、`latencyMs` 与 `lastDiagnostic` 字段；`KeyEntry` 增加 `failureCount`、`cooldownUntil`、`cooldownReason`、`lastUsedAt` 字段。冷却状态由 `cooldownUntil` 推导，并不会自动执行后台轮询；应用在用户打开页面、发起任务、手动测试或再次读取密钥池时恢复到期密钥。这种方式避免了在没有用户明确配置后台服务时持续请求第三方接口。

## MCP 工具与本地记忆

MCP 工具中心仅管理用户在应用内添加的远程服务，不读取或复用任何个人连接器。服务地址、传输类型和认证令牌均由用户显式配置；令牌只保存至设备安全存储，列表仅显示认证状态及尾号。HTTP 服务以 JSON-RPC 请求执行初始化、工具发现与工具调用；SSE 服务保存事件流地址及服务端消息端点，并仅在应用前台进行一次性连接测试，不建立后台常驻连接。

| 实体 | 关键字段 | 用途 |
| --- | --- | --- |
| McpServer | id、name、transport、endpoint、messageEndpoint、enabled、diagnostic | 用户自有 HTTP 或 SSE MCP 服务的非敏感配置及连接状态 |
| McpTool | id、serverId、name、description、inputSchema、enabled、lastStatus | 服务端 `tools/list` 发现的工具及其启用状态 |
| MemoryEntry | id、title、content、category、enabled、createdAt、updatedAt | 设备本地保存、可检索的代理记忆 |
| McpCall | id、serverId、toolName、status、summary、createdAt | 不含凭据或完整请求体的工具调用审计记录 |

用户新增服务时选择 **HTTP** 或 **SSE**。HTTP 服务使用配置的端点执行 JSON-RPC 初始化及 `tools/list`；SSE 服务前台读取事件流响应，并使用服务端给出的消息端点发送同样的 JSON-RPC 请求。每次请求都有超时控制，错误只记录 HTTP 状态和脱敏摘要。调用工具前，用户必须选择工具并输入与该工具 JSON Schema 对应的 JSON 参数。

记忆库允许用户创建、编辑、分类、启用或删除记忆。代理任务仅引用已启用且与输入关键词匹配的少量本地记忆；任务轨迹仅列出引用的标题，不展示完整私密内容。记忆默认使用设备本地持久化保存，删除时立即从本地状态移除。

## 视觉语言

品牌以深色控制台视觉表达“可靠调度”，并以高亮蓝绿色提示可用和执行中状态。页面采用高对比的卡片层级、大号指标数字和紧凑的状态徽章。

| 角色 | 色值 | 使用位置 |
| --- | --- | --- |
| 深海墨黑 | `#08131E` | 页面背景与顶部沉浸区域 |
| 石墨蓝 | `#112433` | 卡片、底部导航、输入容器 |
| 代理青绿 | `#46E0C2` | 主操作、在线状态、进度强调 |
| 电光蓝 | `#5E9BFF` | 选中标签、链接、模型标识 |
| 预警琥珀 | `#F6B75B` | 配额偏低、冷却中与注意状态 |
| 故障珊瑚 | `#FF6D6D` | 调用失败、危险操作与删除确认 |
| 雾白 | `#EAF2F7` | 主文本与关键数字 |

## 可访问性与交互细节

所有状态都会同时通过颜色、文字和图标表达。触控目标不小于 44pt；敏感字段默认脱敏，并以短按查看末四位、长按进行复制的方式减少误触。删除供应商或清除本地配置必须使用底部确认抽屉。
